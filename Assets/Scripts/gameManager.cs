using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class gameManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Psuedo coding since my programming is pretty limited
        //This is my singleton, it's a singleton because there can only be one game manager at all times
        //This singleton will look at the players health as well as the enemies health
        //Making the player and enemies have health with a bool
        
    }

    // Update is called once per frame
    void Update()
    {
        //If the player has no health then destroy.gameObject and display the players score

    }
}
