using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CommandPattern : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //This is my other pattern I'm implementing and it's a command pattern
        //I chose to use the command pattern because it helps with player input handling such as undoing and redoing levels
        //The use of this design pattern within the game will be used to help select levels and replaying levels
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
